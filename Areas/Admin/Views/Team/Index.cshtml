@model ICollection<TeamMember>
@{
    int count = 1;
}

<div class="d-flex justify-content-end mb-2">
    <a class="btn btn-success text-light" asp-area="Admin" asp-controller="Team" asp-action="Create">Create TeamMember</a>
</div>
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                Number
                            </th>
                            <th>
                                TeamMember Fullname
                            </th>
                            <th>
                                Image
                            </th>

                        </tr>
                    </thead>
                    <tbody>

                        @foreach (TeamMember item in Model)
                        {
                            <tr>
                                <td> @count</td>

                                <td>
                                    @item.FullName
                                </td>
                                <td>
                                    @item.Profession
                                </td>
                                <td>
                                   <img scr="~/assets/img/@item.ImagePath"></img>
                                </td>
                                <td>
                                    <a class="btn btn-warning text-light" asp-area="Admin" asp-controller="Team" asp-action="Update" asp-route-Id="@item.Id">Update</a>
                                    <a class="btn btn-danger text-light" asp-area="Admin" asp-controller="Team" asp-action="Delete" asp-route-Id="@item.Id">Delete</a>
                                </td>
                            </tr>
                            count++;
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>